%YAML 1.0
---
# look in cog's system config directory (/etc/cog or /usr/local/etc)
# for all possible settings

local:
  # bind_dn: cn=root,dc=playground,dc=tree
  ldap_encryption: true
  use_keyring: false
